
---

## 🚀 Prerequisites

- Node.js (v16+ recommended)
- npm (comes with Node.js)

Check versions:
```bash
node -v
npm -v

⚙️ Setup Instructions
📍 Step 1: Go to frontend folder
cd frontend

📍 Step 2: Initialize Node project (if not already)
npm init -y


This creates:

package.json

📍 Step 3: Install Express
npm install express


This creates:

node_modules/

package-lock.json

📍 Step 4: Install Nodemon (for auto-restart during development)
npm install nodemon --save-dev
